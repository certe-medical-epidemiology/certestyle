%
%
%
% Zie de Certe-opmaak m.n. vanaf regel 415
%
% Matthijs Berends en Erwin Hassing 
% februari-mei 2022
%
%
%

% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode$for(hyperrefoptions)$,$hyperrefoptions$$endfor$}{hyperref}
\PassOptionsToPackage{hyphens}{url}
$if(colorlinks)$
  \PassOptionsToPackage{dvipsnames,svgnames*,x11names*}{xcolor}
$endif$
$if(dir)$
$if(latex-dir-rtl)$
  \PassOptionsToPackage{RTLdocument}{bidi}
$endif$
$endif$
$if(CJKmainfont)$
  \PassOptionsToPackage{space}{xeCJK}
$endif$
%
\documentclass[twoside, % twoside zorgt ervoor dat tex weet dat er even en oneven paginas zijn voor verticalbars
$if(fontsize)$
  $fontsize$,
$endif$
$if(lang)$
  $babel-lang$,
$endif$
$if(papersize)$
  $papersize$paper,
$endif$
$if(beamer)$
  ignorenonframetext,
$if(handout)$
  handout,
$endif$
$if(aspectratio)$
  aspectratio=$aspectratio$,
$endif$
$endif$
$for(classoption)$
  $classoption$$sep$,
$endfor$
]{$documentclass$}
$if(beamer)$
$if(background-image)$
\usebackgroundtemplate{%
  \includegraphics[width=\paperwidth]{$background-image$}%
}
$endif$
\usepackage{pgfpages}
\setbeamertemplate{caption}[numbered]
\setbeamertemplate{caption label separator}{: }
\setbeamercolor{caption name}{fg=normal text.fg}
\beamertemplatenavigationsymbols$if(navigation)$$navigation$$else$empty$endif$
$for(beameroption)$
\setbeameroption{$beameroption$}
$endfor$
% Prevent slide breaks in the middle of a paragraph
\widowpenalties 1 10000
\raggedbottom
$if(section-titles)$
\setbeamertemplate{part page}{
  \centering
  \begin{beamercolorbox}[sep=16pt,center]{part title}
    \usebeamerfont{part title}\insertpart\par
  \end{beamercolorbox}
}
\setbeamertemplate{section page}{
  \centering
  \begin{beamercolorbox}[sep=12pt,center]{part title}
    \usebeamerfont{section title}\insertsection\par
  \end{beamercolorbox}
}
\setbeamertemplate{subsection page}{
  \centering
  \begin{beamercolorbox}[sep=8pt,center]{part title}
    \usebeamerfont{subsection title}\insertsubsection\par
  \end{beamercolorbox}
}
\AtBeginPart{
  \frame{\partpage}
}
\AtBeginSection{
  \ifbibliography
  \else
    \frame{\sectionpage}
  \fi
}
\AtBeginSubsection{
  \frame{\subsectionpage}
}
$endif$
$endif$
$if(beamerarticle)$
\usepackage{beamerarticle} % needs to be loaded first
$endif$
% $if(fontfamily)$
% \usepackage[$for(fontfamilyoptions)$$fontfamilyoptions$$sep$,$endfor$]{$fontfamily$}
% $else$
% \usepackage{lmodern}
% $endif$
$if(linestretch)$
\usepackage{setspace}
$endif$
\usepackage{amssymb,amsmath}
\usepackage{ifxetex,ifluatex}
\ifnum 0\ifxetex 1\fi\ifluatex 1\fi=0 % if pdftex
  \usepackage[$if(fontenc)$$fontenc$$else$T1$endif$]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
$if(mathspec)$
  \ifxetex
    \usepackage{mathspec}
  \else
    \usepackage{unicode-math}
  \fi
$else$
  \usepackage{unicode-math}
$endif$
\defaultfontfeatures{Scale=MatchLowercase}
\defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
$if(mainfont)$
\setmainfont[$for(mainfontoptions)$$mainfontoptions$$sep$,$endfor$]{$mainfont$}
$else$
\setmainfont{Calibri}
$endif$
$if(sansfont)$
\setsansfont[$for(sansfontoptions)$$sansfontoptions$$sep$,$endfor$]{$sansfont$}
$else$
$if(mainfont)$
\setsansfont[$for(mainfontoptions)$$mainfontoptions$$sep$,$endfor$]{$mainfont$}
$else$
\setsansfont{Calibri}
$endif$
$endif$
$if(monofont)$
\setmonofont[$for(monofontoptions)$$monofontoptions$$sep$,$endfor$]{$monofont$}
$else$
\setmonofont{Fira Code}
$endif$
% $for(fontfamilies)$
%   \newfontfamily{$fontfamilies.name$}[$for(fontfamilies.options)$$fontfamilies.options$$sep$,$endfor$]{$fontfamilies.font$}
% $endfor$
$if(mathfont)$
$if(mathspec)$
  \ifxetex
    \setmathfont(Digits,Latin,Greek)[$for(mathfontoptions)$$mathfontoptions$$sep$,$endfor$]{$mathfont$}
  \else
    \setmathfont[$for(mathfontoptions)$$mathfontoptions$$sep$,$endfor$]{$mathfont$}
  \fi
$else$
  \setmathfont[$for(mathfontoptions)$$mathfontoptions$$sep$,$endfor$]{$mathfont$}
$endif$
$endif$
$if(CJKmainfont)$
  \ifxetex
    \usepackage{xeCJK}
    \setCJKmainfont[$for(CJKoptions)$$CJKoptions$$sep$,$endfor$]{$CJKmainfont$}
  \fi
$endif$
$if(luatexjapresetoptions)$
  \ifluatex
    \usepackage[$for(luatexjapresetoptions)$$luatexjapresetoptions$$sep$,$endfor$]{luatexja-preset}
  \fi
$endif$
$if(CJKmainfont)$
  \ifluatex
    \usepackage[$for(luatexjafontspecoptions)$$luatexjafontspecoptions$$sep$,$endfor$]{luatexja-fontspec}
    \setmainjfont[$for(CJKoptions)$$CJKoptions$$sep$,$endfor$]{$CJKmainfont$}
  \fi
$endif$
\fi
$if(beamer)$
$if(theme)$
\usetheme[$for(themeoptions)$$themeoptions$$sep$,$endfor$]{$theme$}
$endif$
$if(colortheme)$
\usecolortheme{$colortheme$}
$endif$
$if(fonttheme)$
\usefonttheme{$fonttheme$}
$endif$
$if(mainfont)$
\usefonttheme{serif} % use mainfont rather than sansfont for slide text
$endif$
$if(innertheme)$
\useinnertheme{$innertheme$}
$endif$
$if(outertheme)$
\useoutertheme{$outertheme$}
$endif$
$endif$
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[$for(microtypeoptions)$$microtypeoptions$$sep$,$endfor$]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
$if(indent)$
$else$
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
$endif$
$if(verbatim-in-note)$
\usepackage{fancyvrb}
$endif$
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\IfFileExists{bookmark.sty}{\usepackage{bookmark}}{\usepackage{hyperref}}
\hypersetup{
$if(title-meta)$
  pdftitle={$title-meta$},
$endif$
$if(author-meta)$
  pdfauthor={$author-meta$},
$endif$
$if(lang)$
  pdflang=$lang$,
$endif$
$if(subject)$
  pdfsubject={$subject$},
$endif$
$if(keywords)$
  pdfkeywords={$for(keywords)$$keywords$$sep$, $endfor$},
$endif$
$if(colorlinks)$
  colorlinks=true,
  % linkcolor=$if(linkcolor)$$linkcolor$$else$Maroon$endif$,
  % filecolor=$if(filecolor)$$filecolor$$else$Maroon$endif$,
  % citecolor=$if(citecolor)$$citecolor$$else$Blue$endif$,
  % urlcolor=$if(urlcolor)$$urlcolor$$else$Blue$endif$,
$else$
  hidelinks,
$endif$
  pdfcreator={Stichting Certe}}
\urlstyle{same} % disable monospaced font for URLs
$if(verbatim-in-note)$
\VerbatimFootnotes % allow verbatim text in footnotes
$endif$
$if(beamer)$
\geometry{$for(geometry)$$geometry$$sep$,$endfor$}
$else$
\usepackage[a4paper, portrait, top=13mm,bottom=24mm,right=24mm,left=24mm]{geometry}
$endif$
$if(beamer)$
\newif\ifbibliography
$endif$
$if(listings)$
\usepackage{listings}
\newcommand{\passthrough}[1]{#1}
\lstset{defaultdialect=[5.3]Lua}
\lstset{defaultdialect=[x86masm]Assembler}
$endif$
$if(lhs)$
\lstnewenvironment{code}{\lstset{language=Haskell,basicstyle=\small\ttfamily}}{}
$endif$
$if(highlighting-macros)$
$highlighting-macros$
$endif$
$if(tables)$
\usepackage{longtable,booktabs}
$if(beamer)$
\usepackage{caption}
% Make caption package work with longtable
\makeatletter
\def\fnum@table{\tablename~\thetable}
\makeatother
$else$
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
$endif$
$endif$
$if(graphics)$
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother
$endif$
$if(links-as-notes)$
% Make links footnotes instead of hotlinks:
\DeclareRobustCommand{\href}[2]{#2\footnote{\url{#1}}}
$endif$
$if(strikeout)$
\usepackage[normalem]{ulem}
% Avoid problems with \sout in headers with hyperref
\pdfstringdefDisableCommands{\renewcommand{\sout}{}}
$endif$
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
$endif$
$if(beamer)$
$else$
$if(block-headings)$
% Make \paragraph and \subparagraph free-standing
\ifx\paragraph\undefined\else
  \let\oldparagraph\paragraph
  \renewcommand{\paragraph}[1]{\oldparagraph{#1}\mbox{}}
\fi
\ifx\subparagraph\undefined\else
  \let\oldsubparagraph\subparagraph
  \renewcommand{\subparagraph}[1]{\oldsubparagraph{#1}\mbox{}}
\fi
$endif$
$endif$
$if(pagestyle)$
\pagestyle{$pagestyle$}
$endif$
$for(header-includes)$
$header-includes$
$endfor$
% dit stuk is niet meer nodig, wordt verderop gedaan in onze instellingen
% $if(lang)$
% % \ifxetex
%   % Load polyglossia as late as possible: uses bidi with RTL langages (e.g. Hebrew, Arabic)
% %   \usepackage{polyglossia}
% %   \setmainlanguage[$polyglossia-lang.options$]{$polyglossia-lang.name$}
% % $for(polyglossia-otherlangs)$
% %   \setotherlanguage[$polyglossia-otherlangs.options$]{$polyglossia-otherlangs.name$}
% % $endfor$
% % \else
%   \usepackage[shorthands=off,$for(babel-otherlangs)$$babel-otherlangs$,$endfor$main=$babel-lang$]{babel}
% $if(babel-newcommands)$
%   $babel-newcommands$
% $endif$
% % \fi
% $endif$
$if(dir)$
\ifxetex
  % Load bidi as late as possible as it modifies e.g. graphicx
  \usepackage{bidi}
\fi
\ifnum 0\ifxetex 1\fi\ifluatex 1\fi=0 % if pdftex
  \TeXXeTstate=1
  \newcommand{\RL}[1]{\beginR #1\endR}
  \newcommand{\LR}[1]{\beginL #1\endL}
  \newenvironment{RTL}{\beginR}{\endR}
  \newenvironment{LTR}{\beginL}{\endL}
\fi
$endif$
$if(natbib)$
\usepackage[$natbiboptions$]{natbib}
\bibliographystyle{$if(biblio-style)$$biblio-style$$else$plainnat$endif$}
$endif$
$if(biblatex)$
\usepackage[$if(biblio-style)$style=$biblio-style$,$endif$$for(biblatexoptions)$$biblatexoptions$$sep$,$endfor$]{biblatex}
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
$endif$
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newenvironment{CSLReferences}%
  {$if(csl-hanging-indent)$\setlength{\parindent}{0pt}%
  \everypar{\setlength{\hangindent}{\cslhangindent}}\ignorespaces$endif$}%
  {\par}
$endif$

$if(title)$
  \title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$
$if(subtitle)$
  $if(beamer)$
  $else$
    \usepackage{etoolbox}
    \makeatletter
    \providecommand{\subtitle}[1]{% add subtitle to \maketitle
      \apptocmd{\@title}{\par {\large #1 \par}}{}{}
    }
    \makeatother
  $endif$
  \subtitle{$subtitle$}
$endif$
\author{$for(author)$$author$$sep$ \and $endfor$}
\date{$date$}
$if(beamer)$
  \institute{Stichting Certe}
  % $if(institute)$
  %   \institute{$for(institute)$$institute$$sep$ \and $endfor$}
  % $endif$
  $if(titlegraphic)$
    \titlegraphic{\includegraphics{$titlegraphic$}}
  $endif$
  $if(logo)$
    \logo{\includegraphics{$logo$}}
  $endif$
$endif$

% ------------------------------------------------------------------------------------------------- 
% Certe-specifieke instellingen
% ------------------------------------------------------------------------------------------------- 

% Nederlandse namen voor secties in R Markdown (niet nodig voor Quarto)
\usepackage{babel}
\selectlanguage{dutch}
\renewcommand*\contentsname{Inhoudsopgave}

% Voor de strook op de titelpagina
\usepackage{tcolorbox}

% Ondersteunen van landscape pagina's door tekst tussen \begin{landscape} en \end{landscape} te zetten
\usepackage{lscape}

% Deze pakketten zijn nodig voor extra brede tabellen, geeft anders error:
% (https://tex.stackexchange.com/a/631393/195799)
\usepackage{array}
\usepackage{ragged2e}  % for '\RaggedRight' macro
\usepackage{calc}      % for '\widthof' macro
\newcolumntype{P}[1]{>{\RaggedRight\hspace{0pt}}p{#1}}
\newcolumntype{L}{>{\RaggedRight}X}

% Kleuren voor headings en Quarto callouts
\usepackage{xstring}
$if(colour-main)$
% als er een hoofdkleur ingesteld is, deze aan de volgende elementen geven:
\newcommand{\colourTitlepageTitlebanner}{$colour-main$}
\newcommand{\colourVerticalbars}{$colour-main$}
\newcommand{\colourSubtitle}{$colour-main$}
\newcommand{\colourSubtitleTwo}{$colour-main$}
\newcommand{\colourAuthorDate}{$colour-main$}
\newcommand{\colourHeadingOne}{$colour-main$}
% instellingen overschrijven die toch ingesteld staan
$if(colour-titlepage-titlebanner)$\renewcommand{\colourTitlepageTitlebanner}{$colour-titlepage-titlebanner$}$endif$
$if(colour-verticalbars)$\renewcommand{\colourVerticalbars}{$colour-verticalbars$}$endif$
$if(colour-subtitle)$\renewcommand{\colourSubtitle}{$colour-subtitle$}$endif$
$if(colour-subtitle2)$\renewcommand{\colourSubtitleTwo}{$colour-subtitle2$}$endif$
$if(colour-author-date)$\renewcommand{\colourAuthorDate}{$colour-author-date$}$endif$
$if(colour-heading1)$\renewcommand{\colourHeadingOne}{$colour-heading1$}$endif$
$else$
\newcommand{\colourTitlepageTitlebanner}{$if(colour-titlepage-titlebanner)$$colour-titlepage-titlebanner$$else$01617E$endif$} % standaard certeblauw
\newcommand{\colourVerticalbars}{$if(colour-verticalbars)$$colour-verticalbars$$else$01617E$endif$} % standaard certeblauw
\newcommand{\colourSubtitle}{$if(colour-subtitle)$$colour-subtitle$$else$01617E$endif$} % standaard certeblauw
\newcommand{\colourSubtitleTwo}{$if(colour-subtitle2)$$colour-subtitle2$$else$\colourSubtitle$endif$} % standaard colour-subtitle
\newcommand{\colourAuthorDate}{$if(colour-author-date)$$colour-author-date$$else$01617E$endif$} % standaard certeblauw
\newcommand{\colourHeadingOne}{$if(colour-heading1)$$colour-heading1$$else$01617E$endif$} % standaard certeblauw
$endif$
% overige elementen:
\newcommand{\colourTitlepageFull}{$if(colour-titlepage-full)$$colour-titlepage-full$$else$FFFFFF$endif$} % standaard wit
\newcommand{\colourTitle}{$if(colour-title)$$colour-title$$else$FFFFFF$endif$} % standaard wit
\newcommand{\colourIdentifier}{$if(colour-identifier)$$colour-identifier$$else$CBCBCB$endif$} % standaard grijs
\newcommand{\colourHeadingTwo}{$if(colour-heading2)$$colour-heading2$$else$E04883$endif$} % standaard certeroze
\newcommand{\colourHeadingThree}{$if(colour-heading3)$$colour-heading3$$else$01617E$endif$} % standaard certeblauw
\newcommand{\colourUrl}{$if(colour-url)$$colour-url$$else$E04883$endif$} % standaard certeroze
% hastags verwijderen uit HTML
\StrSubstitute{\colourTitlepageTitlebanner}{\#}{}[\colourTitlepageTitlebanner]
\StrSubstitute{\colourTitlepageFull}{\#}{}[\colourTitlepageFull]
\StrSubstitute{\colourVerticalbars}{\#}{}[\colourVerticalbars]
\StrSubstitute{\colourTitle}{\#}{}[\colourTitle]
\StrSubstitute{\colourSubtitle}{\#}{}[\colourSubtitle]
\StrSubstitute{\colourSubtitleTwo}{\#}{}[\colourSubtitleTwo]
\StrSubstitute{\colourAuthorDate}{\#}{}[\colourAuthorDate]
\StrSubstitute{\colourIdentifier}{\#}{}[\colourIdentifier]
\StrSubstitute{\colourHeadingOne}{\#}{}[\colourHeadingOne]
\StrSubstitute{\colourHeadingTwo}{\#}{}[\colourHeadingTwo]
\StrSubstitute{\colourHeadingThree}{\#}{}[\colourHeadingThree]
\StrSubstitute{\colourUrl}{\#}{}[\colourUrl]
% kleuren instellen
\definecolor{colour-titlepage-titlebanner}{HTML}{\colourTitlepageTitlebanner}
\definecolor{colour-titlepage-full}{HTML}{\colourTitlepageFull}
\definecolor{colour-verticalbars}{HTML}{\colourVerticalbars}
\definecolor{colour-title}{HTML}{\colourTitle}
\definecolor{colour-subtitle}{HTML}{\colourSubtitle}
\definecolor{colour-subtitle2}{HTML}{\colourSubtitleTwo}
\definecolor{colour-author-date}{HTML}{\colourAuthorDate}
\definecolor{colour-identifier}{HTML}{\colourIdentifier}
\definecolor{colour-heading1}{HTML}{\colourHeadingOne}
\definecolor{colour-heading2}{HTML}{\colourHeadingTwo}
\definecolor{colour-heading3}{HTML}{\colourHeadingThree}
\definecolor{colour-url}{HTML}{\colourUrl}
% ook wat Quarto callout-vlakken
\definecolor{quarto-callout-caution-color-frame}{HTML}{\colourHeadingTwo}
\definecolor{quarto-callout-caution-color}{HTML}{\colourHeadingTwo}
\definecolor{quarto-callout-color-frame}{HTML}{\colourHeadingOne}
\definecolor{quarto-callout-color}{HTML}{\colourHeadingOne}
\definecolor{quarto-callout-important-color-frame}{HTML}{\colourHeadingTwo}
\definecolor{quarto-callout-important-color}{HTML}{\colourHeadingTwo}
\definecolor{quarto-callout-note-color-frame}{HTML}{\colourHeadingOne}
\definecolor{quarto-callout-note-color}{HTML}{\colourHeadingOne}
\definecolor{quarto-callout-tip-color-frame}{HTML}{\colourHeadingThree}
\definecolor{quarto-callout-tip-color}{HTML}{\colourHeadingThree}
\definecolor{quarto-callout-warning-color-frame}{HTML}{\colourHeadingTwo}
\definecolor{quarto-callout-warning-color}{HTML}{\colourHeadingTwo}

% captions van grafieken en tabellen links uitgelijnd, punt ipv dubbele punt ("Figuur 1." ipv "Figuur 1:") en Figuur/Tabel in vet
\usepackage{caption}
\captionsetup{labelsep=period, justification=justified, singlelinecheck=false, labelfont=bf}

% logos
\newcommand{\titlepageLogo}{}
$if(logofront)$
\renewcommand{\titlepageLogo}{$logofront$}
$endif$
$if(titlepage-logo)$
\renewcommand{\titlepageLogo}{$titlepage-logo$}
$endif$

\newcommand{\footerLogo}{}
$if(logofooter)$
\renewcommand{\footerLogo}{$logofooter$}
$endif$
$if(footer-logo)$
\renewcommand{\footerLogo}{$footer-logo$}
$endif$

\usepackage{sectsty}
% kop 1
\sectionfont{\newpage\color{colour-heading1}\selectfont}
% kop 2
\subsectionfont{\color{colour-heading2}\selectfont}
% kop 3
\subsubsectionfont{\color{colour-heading3}\selectfont}

% dit zorgt ervoor dat het toegestaan is om whitespace onderaan pagina's te laten;
% we gebruiken `\documentclass[twoside, ...]`, wat normaal dit niet toelaat en pagina-inhoud 'stretcht'
\raggedbottom

% kleur van links
\usepackage[normalem]{ulem} % underlining
\hypersetup{
  colorlinks = true,
  urlcolor = colour-url, % external hyperlinks
  filecolor = colour-url, % file links
  linkcolor = black, % internal links (like ToC)
  citecolor = black, % citations
}
\renewcommand{\url}[1]{\textcolor{colour-url}{\urlstyle{same}\href{#1}{#1}}}
\let\oldhref\href
\renewcommand{\href}[2]{\textcolor{colour-url}{\underline{\oldhref{#1}{#2}}}}

% footer opmaken met paginanummers en klein Certe-logo
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{ifthen}
\pagestyle{fancy}
\fancyhf{} % % header en footer leeghalen, anders komen koptitels in the header
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
% verticale balken links en rechts (14 mm komt uit definitie a4paper eerder (24 - 10 = 14)
\fancyhead[LO]{\makebox[-14mm][r]{\color{colour-verticalbars}\rule{10mm}{\paperheight}}} % LE = left, odd
\fancyhead[RE]{\makebox[-14mm][l]{\color{colour-verticalbars}\rule{10mm}{\paperheight}}} % RO = right, even

% paginanummers in footer, standaard ' van ' tussen paginanummers
\newcommand{\translateOf}
  {\ifthenelse{\equal{$lang$}{}}{ van } % standaard 'van' als lang ontbreekt
  {\ifthenelse{\equal{$lang$}{nl}}{ van }
  {\ifthenelse{\equal{$lang$}{de}}{ von }
  { of }}}}
\cfoot{$if(remove-footer-page-numbers)$$else$\number\value{page}\translateOf\pageref{LastPage}$endif$\\
       \ifthenelse{\equal{\footerLogo}{}}{}{\includegraphics[height=7mm,keepaspectratio]{\footerLogo}}}

\begin{document}
% paginanummering uitzetten (voor 1e pagina):
\pagenumbering{gobble}
$if(has-frontmatter)$
  \frontmatter
$endif$
$if(title)$ % dus er komt geen voorpagina als er geen titel ingevuld is
  $if(beamer)$
    \frame{\titlepage}
  $else$
    % \maketitle % = oude manier, hieronder aangepast voor mooie voorpagina:
    \begin{titlepage}
      \pagecolor{colour-titlepage-full}

      \begin{center}
        \vspace*{-19mm}
        \ifthenelse{\equal{\titlepageLogo}{}}{}{\includegraphics[width=210mm,height=70mm,keepaspectratio]{\titlepageLogo}}
        
        \vfill

        \begin{tcolorbox}[size=tight,oversize,sharp corners,
          colback=colour-titlepage-titlebanner,
          colframe=colour-titlepage-titlebanner,
          left=100pt,  % interne margin in de tekstbox
          right=100pt, % interne margin in de tekstbox
          toprule=14mm, % dikte van de rand (= bovenkant banner tot aan tekst)
          bottomrule=14mm, % dikte van de rand (= onderkant banner tot aan tekst)
          fontupper=\Huge,
          halign=right] % tekst rechts uitlijnen
          \textcolor{colour-title}{\textbf{$title$}}
        \end{tcolorbox}
        
        \vspace{5mm}
        
        \LARGE % dit is iets groter dan \large
        \hspace*{\fill}\textcolor{colour-subtitle}{\textbf{$subtitle$}}

        \vspace{5mm}
        
        \large % dit is iets kleiner dan \LARGE
        \hspace*{\fill}\textcolor{colour-subtitle2}{\textbf{$subtitle2$}}
        
        \vfill
        
      \end{center}

      \hspace*{\fill}\large\textcolor{colour-author-date}{\textbf{$date$}}
      
      \scriptsize % dit is iets kleiner dan \small, maar groter dan \tiny
      \textcolor{colour-identifier}{\textbf{$identifier$}}
      \hspace*{\fill}\large\textcolor{colour-author-date}{\textbf{$author$}}
      
      \vspace*{-10mm}
      
    \end{titlepage}
  $endif$

  \newpage
  \pagecolor{white}

  $if(abstract)$
  \begin{abstract}
  $abstract$
  \newpage
  \end{abstract}
  $endif$
$endif$

% paginanummering beginnen:
\pagenumbering{arabic} 

$for(include-before)$
$include-before$
$endfor$
$if(toc)$
$if(toc-title)$
%\renewcommand*\contentsname{Inhoudsopgave} % niet nodig voor Quarto (gaat met 'lang'), alleen Rmd
$endif$
$if(beamer)$
\begin{frame}[allowframebreaks]
$if(toc-title)$
%\frametitle{Inhoudsopgave} % niet nodig voor Quarto (gaat met 'lang'), alleen Rmd
$endif$
\tableofcontents[hideallsubsections]
\end{frame}
$else$
{
$if(colorlinks)$
\hypersetup{linkcolor=$if(toccolor)$$toccolor$$else$$endif$}
$endif$
\setcounter{tocdepth}{$toc-depth$}
\tableofcontents
}
$endif$
$endif$

\newpage

$if(lot)$
\listoftables
\newpage
$endif$
$if(lof)$
\listoffigures
\newpage
$endif$
$if(linestretch)$
\setstretch{$linestretch$}
$endif$
$if(has-frontmatter)$
\mainmatter
$endif$
$body$

$if(has-frontmatter)$
\backmatter
$endif$
$if(natbib)$
$if(bibliography)$
$if(biblio-title)$
$if(has-chapters)$
\renewcommand\bibname{$biblio-title$}
$else$
\renewcommand\refname{$biblio-title$}
$endif$
$endif$
$if(beamer)$
\begin{frame}[allowframebreaks]{$biblio-title$}
  \bibliographytrue
$endif$
  \bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
$if(beamer)$
\end{frame}
$endif$

$endif$
$endif$
$if(biblatex)$
$if(beamer)$
\begin{frame}[allowframebreaks]{$biblio-title$}
  \bibliographytrue
  \printbibliography[heading=none]
\end{frame}
$else$
\printbibliography$if(biblio-title)$[title=$biblio-title$]$endif$
$endif$

$endif$
$for(include-after)$
$include-after$

$endfor$
\end{document}
